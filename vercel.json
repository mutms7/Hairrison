{
  "version": 2,
  "buildCommand": "npm --prefix v2 run build",
  "devCommand": "npm --prefix v2 run dev",
  "installCommand": "npm install && npm install --prefix v2",
  "outputDirectory": "v2/dist"
}
