{
  "name": "hairrison",
  "private": true,
  "description": "AI-powered hair try-on — v2 (Hairrison Studio) with one free look and a $0.99 unlimited unlock",
  "scripts": {
    "dev": "npm --prefix v2 run dev",
    "build": "npm --prefix v2 run build",
    "preview": "npm --prefix v2 run preview",
    "dev:v1": "npm --prefix Hairrison run dev",
    "build:v1": "npm --prefix Hairrison run build"
  },
  "dependencies": {
    "stripe": "^17.0.0"
  }
}
