{
  "name": "hairrison",
  "private": true,
  "description": "AI-powered hair try-on visualization tool",
  "scripts": {
    "dev": "cd Hairrison && npm run dev",
    "build": "cd Hairrison && npm run build",
    "preview": "cd Hairrison && npm run preview",
    "lint": "cd Hairrison && npm run lint"
  },
  "dependencies": {
    "cloudinary": "^2.9.0"
  }
}
